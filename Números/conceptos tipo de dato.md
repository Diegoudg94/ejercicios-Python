# tipos de datos "hola" (str) es un texto o string 2234 son numeros son interger (int) no deben ir entre comillas
#floating (float) son numeros con decimales
#list (list) [se escriben entre corchetes] [sal, pimienta, azucar]
#Diccionarios (dic) {se escriben entre llaves} {'color', 'rojo', 'azul'}
#tuples (tuple) son elementos que tienn un orden que no se modifican nunca ('lun', 'mar', 'Viernes')
#set (set) elementos unicos e irrepetibles  {'a','b', 'c'}
#booleanos (bool) true, false

